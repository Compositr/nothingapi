{
  "name": "nothingapi",
  "version": "0.0.4",
  "description": "A JS wrapper for the Nothing API",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [
    "nothing",
    "nothingapi"
  ],
  "author": "Compositr",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.9.4"
  },
  "files": [
    "./lib/**/*"
  ],
  "engines": {
    "node": ">=17.5.0"
  }
}
